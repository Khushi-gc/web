---
const cards = [
  {
    title: "Study Abroad",
    desc: "Mix & Match modules: Discovery + Scholarships + Loans + Visa Support",
    iconPath:
      "M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-1 17.93c-3.95-.49-7-3.85-7-7.93 0-.62.08-1.21.21-1.79L9 15v1c0 1.1.9 2 2 2v1.93zm6.9-2.54c-.26-.81-1-1.39-1.9-1.39h-1v-3c0-.55-.45-1-1-1H8v-2h2c.55 0 1-.45 1-1V7h2c1.1 0 2-.9 2-2v-.41c2.93 1.19 5 4.06 5 7.41 0 2.08-.8 3.97-2.1 5.39z",
    viewBox: "0 0 24 24",
  },
  {
    title: "Global Employment",
    desc: "Mix & Match modules: Up-skilling + Internships + Profile Building + Language test",
    iconPath:
      "M16 6l2.29 2.29-4.88 4.88-4-4L2 16.59 3.41 18l6-6 4 4 6.3-6.29L22 12V6z",
    viewBox: "0 0 24 24",
  },
];
---

<section class="ecosystem-section">
  <div class="container">
    <div class="header-section">
      <h2 class="section-title">
        How the Ecosystem<br /> Optimizes Your Career
      </h2>
      <div class="section-desc">
        <p>
          In a traditional model, you have to find a loan, then find a tutor,
          then find an agent, then find a job. In the Aveka Ecosystem, these
          "modules" talk to each other to accelerate your success.
        </p>
        <p class="mt-4"><strong>If your goal is...</strong></p>
      </div>
    </div>

    <div class="cards-grid">
      {
        cards.map((card, index) => (
          <div class="safety-card" data-index={index}>
            <div class="card-icon-wrapper">
              <svg
                xmlns="http://www.w3.org/2000/svg"
                viewBox={card.viewBox}
                class="card-icon"
              >
                <path
                  fill-rule="evenodd"
                  d={card.iconPath}
                  clip-rule="evenodd"
                />
              </svg>
            </div>
            <div class="card-text">
              <h3>{card.title}</h3>
              <p>{card.desc}</p>
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>

<style>
  .ecosystem-section {
    padding: 6rem 0;
    background: white;
    color: #1a1a1a;
  }

  .mt-4 {
    margin-top: 1rem;
    color: #6200ee;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  @media (max-width: 768px) {
    .ecosystem-section {
      padding: 4rem 0;
    }
    .container {
      padding: 0 1.5rem;
    }
  }

  /* Header */
  .header-section {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    margin-bottom: 4rem;
    padding-bottom: 2rem;
    border-bottom: 0.5px solid rgba(0, 0, 0, 0.1);
  }

  @media (min-width: 1024px) {
    .header-section {
      flex-direction: row;
      justify-content: space-between;
      align-items: flex-start;
    }
  }

  .section-title {
    font-size: 2.25rem;
    font-weight: 700;
    line-height: 1.1;
    text-transform: uppercase;
    color: #1a1a1a;
    max-width: 650px;
  }

  @media (min-width: 1024px) {
    .section-title {
      font-size: 3rem;
    }
  }

  .section-desc {
    max-width: 500px;
  }

  .section-desc p {
    font-size: 1.125rem;
    color: #6b7280;
    line-height: 1.5;
  }

  /* Cards */
  .cards-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }

  @media (min-width: 768px) {
    .cards-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .safety-card {
    background: white;
    border: 1px solid #e5e7eb;
    border-radius: 0.75rem;
    padding: 2rem;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    min-height: 350px;
  }

  .safety-card:hover {
    background: #6200ee;
    transform: translateY(-10px);
    box-shadow: 0 25px 50px -12px rgba(98, 0, 238, 0.4);
    border-color: #6200ee;
  }

  .safety-card:hover .card-icon {
    fill: white;
    transform: scale(1.1) rotate(5deg);
  }

  .safety-card:hover .card-text h3,
  .safety-card:hover .card-text p {
    color: white;
  }

  .card-icon-wrapper {
    margin-bottom: 2rem;
  }

  .card-icon {
    width: 5rem;
    height: 5rem;
    fill: #6200ee; /* Default Purple */
    transition: all 0.3s ease;
  }

  .card-text h3 {
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: #1a1a1a;
    transition: color 0.3s ease;
  }

  .card-text p {
    font-size: 1.1rem;
    color: #6b7280;
    line-height: 1.6;
    transition: color 0.3s ease;
  }

  /* Active State */
  .safety-card.active {
    background: #6200ee;
    border-color: #6200ee;
    transform: translateY(-5px);
    box-shadow: 0 20px 25px -5px rgba(98, 0, 238, 0.3);
  }

  .safety-card.active .card-icon {
    fill: white;
  }

  .safety-card.active .card-text h3,
  .safety-card.active .card-text p {
    color: white;
  }
</style>

<script>
  // Simple toggle logic
  document.addEventListener("DOMContentLoaded", () => {
    const cards = document.querySelectorAll(".safety-card");

    cards.forEach((card) => {
      card.addEventListener("click", () => {
        // Remove active from all others
        cards.forEach((c) => c.classList.remove("active"));
        // Toggle current
        card.classList.add("active");
      });
    });
  });
</script>
