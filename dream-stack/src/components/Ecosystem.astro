---
const cards = [
  {
    title: "Study Abroad",
    desc: "Mix & Match modules: Discovery + Scholarships + Loans + Visa Support",
    iconPath:
      "M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-1 17.93c-3.95-.49-7-3.85-7-7.93 0-.62.08-1.21.21-1.79L9 15v1c0 1.1.9 2 2 2v1.93zm6.9-2.54c-.26-.81-1-1.39-1.9-1.39h-1v-3c0-.55-.45-1-1-1H8v-2h2c.55 0 1-.45 1-1V7h2c1.1 0 2-.9 2-2v-.41c2.93 1.19 5 4.06 5 7.41 0 2.08-.8 3.97-2.1 5.39z",
    viewBox: "0 0 24 24",
    doodles: [
      {
        path: "M12 3L1 9l11 6 9-4.91V17h2V9L12 3z", // Cap
        top: "10%",
        right: "10%",
        rotate: "15deg",
        delay: "0s",
      },
      {
        path: "M21 16v-2l-8-5V3.5c0-.83-.67-1.5-1.5-1.5S10 2.67 10 3.5V9l-8 5v2l8-2.5V19l-2 1.5V22l3.5-1 3.5 1v-1.5L13 19v-5.5l8 2.5z", // Plane
        bottom: "15%",
        right: "15%",
        rotate: "-20deg",
        delay: "0.2s",
      },
      {
        path: "M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 9.19 8.63 2 9.24l5.46 4.73L5.82 21z", // Star
        top: "40%",
        right: "5%",
        rotate: "10deg",
        delay: "0.4s",
      },
    ],
  },
  {
    title: "Global Employment",
    desc: "Mix & Match modules: Up-skilling + Internships + Profile Building + Language test",
    iconPath:
      "M16 6l2.29 2.29-4.88 4.88-4-4L2 16.59 3.41 18l6-6 4 4 6.3-6.29L22 12V6z",
    viewBox: "0 0 24 24",
    doodles: [
      {
        path: "M20 6h-4V4c0-1.11-.89-2-2-2h-4c-1.11 0-2 .89-2 2v2H4c-1.11 0-1.99.89-1.99 2L2 19c0 1.11.89 2 2 2h16c1.11 0 2-.89 2-2V8c0-1.11-.89-2-2-2zm-6 0h-4V4h4v2z", // Briefcase
        top: "10%",
        right: "10%",
        rotate: "-15deg",
        delay: "0.1s",
      },
      {
        path: "M15.5 14h-.79l-.28-.27C15.41 12.59 16 11.11 16 9.5 16 5.91 13.09 3 9.5 3S3 5.91 3 9.5 5.91 16 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z", // Search
        bottom: "10%",
        right: "20%",
        rotate: "20deg",
        delay: "0.3s",
      },
      {
        path: "M12 2l2.4 7.2L22 12l-7.6 2.4L12 22l-2.4-7.6L2 12l7.6-2.4z", // Sparkle
        top: "50%",
        right: "5%",
        rotate: "-5deg",
        delay: "0.5s",
      },
    ],
  },
];
---

<section class="ecosystem-section">
  <div class="container">
    <div class="header-section">
      <h2 class="section-title">
        How the Ecosystem<br /> Optimizes Your Career
      </h2>
      <div class="section-desc">
        <p>
          In a traditional model, you have to find a loan, then find a tutor,
          then find an agent, then find a job. In the Aveka Ecosystem, these
          "modules" talk to each other to accelerate your success.
        </p>
        <p class="mt-4"><strong>If your goal is...</strong></p>
      </div>
    </div>

    <div class="cards-grid">
      {
        cards.map((card, index) => (
          <div class="safety-card" data-index={index}>
            <div class="card-icon-wrapper">
              <svg
                xmlns="http://www.w3.org/2000/svg"
                viewBox={card.viewBox}
                class="card-icon"
              >
                <path
                  fill-rule="evenodd"
                  d={card.iconPath}
                  clip-rule="evenodd"
                />
              </svg>
            </div>
            <div class="card-text">
              <h3>{card.title}</h3>
              <p>{card.desc}</p>
            </div>

            {card.doodles.map((doodle) => (
              <div
                class="doodle"
                style={`top: ${doodle.top || "auto"}; bottom: ${doodle.bottom || "auto"}; right: ${doodle.right || "auto"}; --rotate: ${doodle.rotate}; --delay: ${doodle.delay};`}
              >
                <svg viewBox="0 0 24 24">
                  <path d={doodle.path} />
                </svg>
              </div>
            ))}
          </div>
        ))
      }
    </div>
  </div>
</section>

<style>
  .ecosystem-section {
    padding: 6rem 0;
    background: white;
    color: #1a1a1a;
  }

  .mt-4 {
    margin-top: 1rem;
    color: #660ef0;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  @media (max-width: 768px) {
    .ecosystem-section {
      padding: 4rem 0;
    }
    .container {
      padding: 0 1.5rem;
    }
  }

  /* Header */
  .header-section {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    margin-bottom: 4rem;
    padding-bottom: 2rem;
    border-bottom: 0.5px solid rgba(0, 0, 0, 0.1);
  }

  @media (min-width: 1024px) {
    .header-section {
      flex-direction: row;
      justify-content: space-between;
      align-items: flex-start;
    }
  }

  .section-title {
    font-size: 2.25rem;
    font-weight: 700;
    line-height: 1.1;
    text-transform: uppercase;
    color: #1a1a1a;
    max-width: 650px;
  }

  @media (min-width: 1024px) {
    .section-title {
      font-size: 3rem;
    }
  }

  .section-desc {
    max-width: 500px;
  }

  .section-desc p {
    font-size: 1.125rem;
    color: #6b7280;
    line-height: 1.5;
  }

  /* Cards */
  .cards-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }

  @media (min-width: 768px) {
    .cards-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .safety-card {
    background: white;
    border: 1px solid #e5e7eb;
    border-radius: 0.75rem;
    padding: 2rem;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    min-height: 350px;
    position: relative;
    overflow: hidden;
  }

  .safety-card:hover,
  .safety-card.active {
    background: #660ef0;
    transform: translateY(-10px);
    box-shadow: 0 25px 50px -12px rgba(102, 14, 240, 0.4);
    border-color: #660ef0;
  }

  .safety-card:hover .card-icon,
  .safety-card.active .card-icon {
    fill: white;
    transform: scale(1.1) rotate(5deg);
  }

  .safety-card:hover .card-text h3,
  .safety-card:hover .card-text p,
  .safety-card.active .card-text h3,
  .safety-card.active .card-text p {
    color: white;
  }

  .card-icon-wrapper {
    margin-bottom: 2rem;
  }

  .card-icon {
    width: 5rem;
    height: 5rem;
    fill: #660ef0; /* Default Purple */
    transition: all 0.3s ease;
  }

  .card-text h3 {
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: #1a1a1a;
    transition: color 0.3s ease;
  }

  .card-text p {
    font-size: 1.1rem;
    color: #6b7280;
    line-height: 1.6;
    transition: color 0.3s ease;
  }

  /* Active State - Merged above but keeping explicit rule if overrides needed (none needed) */

  /* Doodles */
  .doodle {
    position: absolute;
    width: 60px;
    height: 60px;
    opacity: 0;
    transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    pointer-events: none;
    transform: rotate(var(--rotate)) scale(0.5);
    z-index: 0;
  }

  .doodle svg {
    width: 100%;
    height: 100%;
    fill: none;
    stroke: white;
    stroke-width: 1.2;
    stroke-linecap: round;
    stroke-linejoin: round;
    filter: drop-shadow(0 0 8px rgba(255, 255, 255, 0.4));
  }

  .safety-card:hover .doodle,
  .safety-card.active .doodle {
    opacity: 0.25;
    transform: rotate(var(--rotate)) scale(1.5);
    animation: doodle-float 4s ease-in-out infinite;
    animation-delay: var(--delay);
  }

  @keyframes doodle-float {
    0%,
    100% {
      transform: rotate(var(--rotate)) scale(1.5) translateY(0);
    }
    50% {
      transform: rotate(var(--rotate)) scale(1.6) translateY(-15px);
    }
  }

  .card-icon-wrapper,
  .card-text {
    position: relative;
    z-index: 1;
  }
</style>

<script>
  // Simple toggle logic
  document.addEventListener("DOMContentLoaded", () => {
    const cards = document.querySelectorAll(".safety-card");

    cards.forEach((card) => {
      card.addEventListener("click", () => {
        // Remove active from all others
        cards.forEach((c) => c.classList.remove("active"));
        // Toggle current
        card.classList.add("active");
      });
    });
  });
</script>