---
import card1 from "../assets/images/card1.webp";
import card2 from "../assets/images/card2.webp";
import card3 from "../assets/images/card3.webp";

const cards = [
    {
        title: 'The "Academic Bridge" Journey',
        student: "Aarav (India) 🇮🇳",
        details: [
            "Goal: Master’s degree in Germany",
            "Discovery: Tuition-free universities",
            "Test Prep: 7.5 IELTS score",
            "Loan: Secured blocked account funding",
            "Outcome: Enrolled in Master's program"
        ],
        image: card1,
    },
    {
        title: 'The "Certified Specialist" Journey',
        student: "Camila (Brazil) 🇧🇷",
        details: [
            "Goal: Upskill via intl. certification",
            "Counseling: Determined career impact",
            "Internships: Field-tied experience",
            "Profile: Refined global CV",
            "Outcome: Role at international firm"
        ],
        image: card2,
    },
    {
        title: 'The "Scholarship Hunter" Journey',
        student: "Kofi (Ghana) 🇬🇭",
        details: [
            "Goal: Study in USA with grants",
            "Funding: $12k grants + loan",
            "Support: Perfected SOP/LORs",
            "Visa: F-1 guidance provided",
            "Outcome: Bachelor's with 80% covered"
        ],
        image: card3,
    },
    {
        title: 'The "Reskilling Success" Journey',
        student: "Sibu (South Africa) 🇿🇦",
        details: [
            "Goal: Transition into Data Science",
            "Discovery: Found top-tier program",
            "Loan: Specialized funding secured",
            "Up-Skilling: Career boosting internships",
            "Outcome: Pivoted to high-paying tech role"
        ],
        image: card1,
    },
];
---

<section class="success-stories-flip" id="success-stories">
    
    <div class="header-container">
        <h2 class="main-header">
            These stories now emphasize that every step is tied to an educational goal.
        </h2>
    </div>

    <div class="cards-grid">
        {cards.map((card) => (
            <div class="flip-card-container">
                <div class="flip-card">
                    <div class="card-front">
                        <figure>
                            <div class="img-bg"></div>
                            <img src={card.image.src} alt={card.title} />
                            <figcaption>
                                {card.title}
                                <div class="student-info">{card.student}</div>
                            </figcaption>
                        </figure>
                        <ul>
                             <!-- Front Detail Decorative -->
                              <li>{card.student}</li>
                         </ul>
                    </div>

                    <div class="card-back">
                        <figure>
                            <div class="img-bg"></div>
                        </figure>

                        <ul>
                            {card.details.map(detail => (
                                <li>{detail}</li>
                            ))}
                        </ul>


                    </div>
                </div>
            </div>
        ))}
    </div>
</section>

<style>
    .success-stories-flip {
        background-color: #ffffff;
        color: #1a1a1a;
        padding: 6rem 2rem;
        font-family: "Inter", sans-serif;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .header-container {
        margin-bottom: 4rem;
        text-align: center;
        max-width: 800px;
    }

    .main-header {
        font-size: clamp(2.2rem, 4vw, 3.8rem);
        font-weight: 800;
        line-height: 1.1;
        margin: 0;
        color: #1a1a1a;
        letter-spacing: -0.01em;
        text-transform: uppercase; /* Optional, often pairs well with this weight/size in these designs */
    }

    .cards-grid {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 2rem;
        width: 100%;
        max-width: 1400px;
    }

    @media (max-width: 768px) {
        .success-stories-flip {
            padding: 4rem 1.5rem;
        }
    }

    /* Flip Card CSS specific */
    .flip-card-container {
        --hue: 260; /* Brand Purple approx */
        --primary: hsl(var(--hue), 60%, 55%);
        --white-1: hsl(0, 0%, 95%);
        --white-2: hsl(0, 0%, 85%);
        --dark: #ffffff; /* Changed to white */
        --grey: hsl(0, 0%, 50%);
        --text-main: #1a1a1a;
        --text-sub: #4b5563;

        width: 310px;
        height: 500px;
        perspective: 1000px;
    }

    .flip-card {
        width: inherit;
        height: inherit;
        position: relative;
        transform-style: preserve-3d;
        transition: .6s .1s;
    }

    /* Hover State */
    .flip-card-container:hover .flip-card,
    .flip-card-container:focus-within .flip-card {
        transform: rotateY(180deg);
    }

    .card-front,
    .card-back {
        width: 100%;
        height: 100%;
        border-radius: 24px;
        background: #ffffff;
        border: 1px solid #e5e7eb; /* Add border for visibility on white bg */
        box-shadow: 0 10px 30px -5px rgba(0, 0, 0, 0.1);
        position: absolute;
        top: 0;
        left: 0;
        overflow: hidden;
        backface-visibility: hidden;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .card-front {
        transform: rotateY(0deg);
        z-index: 2;
        border: none; /* Image covers it usually, but keeping clean */
    }

    .card-back {
        transform: rotateY(180deg);
        z-index: 1;
    }

    figure {
        z-index: -1;
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        margin: 0;
    }

    .img-bg {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: hsla(var(--hue), 60%, 95%, 0.5); /* Lighter purple tint */
    }

    img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        border-radius: 24px;
    }

    figcaption {
        display: block;
        width: auto;
        margin-top: 12%;
        padding: 8px 16px;
        font-weight: 800; /* Bolder */
        font-size: 1rem; /* Slightly larger */
        line-height: 1.4;
        letter-spacing: 0.5px;
        text-align: right;
        position: absolute;
        top: 0;
        right: 12px;
        color: var(--white-1);
        background: hsla(var(--hue), 25%, 10%, .8);
        border-radius: 8px;
        backdrop-filter: blur(4px);
    }

    .student-info {
        display: block;
        font-size: 0.8em;
        font-weight: normal;
        margin-top: 4px;
        opacity: 0.9;
    }

    .card-front .img-bg {
        clip-path: polygon(0 20%, 100% 40%, 100% 100%, 0 100%);
        background: hsla(var(--hue), 25%, 10%, .5); /* Keep front dark-ish or standard for contrast if needed, but usually image is there. */
    }

    .card-front .img-bg::before {
        content: "";
        position: absolute;
        top: 34%;
        left: 50%;
        transform: translate(-50%, -50%) rotate(18deg);
        width: 100%;
        height: 6px;
        border: 1px solid var(--primary);
        border-left-color: transparent;
        border-right-color: transparent;
        transition: .1s;
    }

    .card-back .img-bg {
        clip-path: polygon(0 0, 100% 0, 100% 80%, 0 60%);
        background: linear-gradient(to bottom right, #f3e8ff, #ffffff);
    }

    .flip-card-container:hover .card-front .img-bg::before {
        width: 6px;
        border-left-color: var(--primary);
        border-right-color: var(--primary);
    }

    /* List Content on Back */
    ul {
        padding-top: 20%;
        margin: 0 auto;
        width: 80%;
        height: 100%;
        list-style: none;
        color: var(--text-main);
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        z-index: 10;
        padding-inline-start: 0;
    }

    li {
        width: 100%;
        margin-top: 12px;
        padding-bottom: 12px;
        font-size: 1.1rem; /* Matched to myhub description */
        line-height: 1.7;   /* Matched to myhub description */
        text-align: center;
        position: relative;
        font-weight: 500;
    }

    li:nth-child(2n) {
        color: var(--text-sub);
    }

    li:not(:last-child)::after {
        content: "";
        position: absolute;
        bottom: 0;
        left: 0;
        width: 100%;
        height: 1px;
        background: #e5e7eb;
        opacity: 1;
    }
</style>